<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application

        android:largeHeap="true" >

        <!-- android:authorities="com.example.aymen.androidchat.sql.contentprovider" est un nom arbitraire mais UNIQUE -->
        <!-- for sql operations -->
        <!-- authority for db 'chat.db' -->
        <provider
            android:name=".sql.FileDbContentProvider"
            android:authorities="com.example.aymen.androidchat.sql.contentprovider">
        </provider>
        <!-- authority for 'users table
        <provider
            android:name=".sql.FileDbContentProviderUsers"
            android:authorities="com.example.aymen.androidchat.sql.contentprovideruser">
        </provider>
        -->

        <!--
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChatBoxActivity"></activity>
        <activity android:name=".BlankFragment"></activity>
        -->
    </application>

</manifest>